enum Role {
  ADMIN
  MEMBER
}

enum IdeaStatus {
  DRAFT
  UNDER_REVIEW
  APPROVED
  REJECTED
}

enum VoteType {
  UPVOTE
  DOWNVOTE
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    CANCELLED
}